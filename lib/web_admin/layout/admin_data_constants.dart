/// Shared copy for database-driven admin UI states.
abstract final class AdminDataConstants {
  /// Shown when a PostgreSQL-backed list or table has zero rows.
  static const String emptyRecordsMessage = 'No records found yet.';

  static const String emptyRecordsTitle = 'No records found';
}
