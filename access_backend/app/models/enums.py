"""String constants for user status and role names (matches `roles` seed data)."""


class UserStatus:
    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"


class RoleName:
    ADMIN = "Admin"
    MEMBER = "Member"
    ORGANIZATION = "Organization"
